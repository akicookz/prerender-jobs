steps:
  # Build image tagged with latest
  - name: gcr.io/cloud-builders/docker
    args:
      - "build"
      - "-t"
      - "gcr.io/$PROJECT_ID/prerender-jobs:latest"
      - "."

  # Push latest
  - name: gcr.io/cloud-builders/docker
    args: ["push", "gcr.io/$PROJECT_ID/prerender-jobs:latest"]

  # Update both Cloud Run Jobs to the freshly pushed image
  - name: gcr.io/cloud-builders/gcloud
    args:
      [
        "run",
        "jobs",
        "update",
        "prerender-jobs",
        "--image=gcr.io/$PROJECT_ID/prerender-jobs:latest",
        "--region=us-east1",
        "--project=$PROJECT_ID",
      ]

  - name: gcr.io/cloud-builders/gcloud
    args:
      [
        "run",
        "jobs",
        "update",
        "prerender-jobs-enterprise",
        "--image=gcr.io/$PROJECT_ID/prerender-jobs:latest",
        "--region=us-central1",
        "--project=$PROJECT_ID",
      ]

options:
  logging: CLOUD_LOGGING_ONLY
